# Variables de entorno para la suite de tests
# Copiar como .env.test y ajustar según tu entorno local

# BD de test — NUNCA usar la BD de producción o staging aquí
TEST_DB_URL=postgresql+asyncpg://postgres:ErpLegal2024!@localhost:5432/erp_legal_test
TEST_DB_PORT=5432

# Si el puerto de tu PostgreSQL Docker está mapeado a otro puerto:
# TEST_DB_PORT=5433
